import SwiftUI

enum AppColor {
    // Core Brand Colors
    static let primary         = Color("Colors/Semantic/Primary")
    static let secondary       = Color("Colors/Semantic/Secondary")
    static let accent          = Color("Colors/Semantic/Accent")

    // Surfaces & Backgrounds
    static let background      = Color("Colors/Semantic/Background")
    static let surface         = Color("Colors/Semantic/Surface")
    static let surfaceElevated = Color("Colors/Semantic/SurfaceElevated")

    // Text & Labels
    static let label           = Color("Colors/Semantic/Label")
    static let secondaryLabel  = Color("Colors/Semantic/SecondaryLabel")
    static let tertiaryLabel   = Color("Colors/Semantic/TertiaryLabel")

    // Controls & Lines
    static let separator       = Color("Colors/Semantic/Separator")
    static let fill            = Color("Colors/Semantic/Fill")
    
    // Shadows
    static let shadowLight     = Color("Colors/Semantic/ShadowLight")
    static let shadowDark      = Color("Colors/Semantic/ShadowDark")

    // Status Colors
    static let success         = Color("Colors/Status/Success")
    static let warning         = Color("Colors/Status/Warning")
    static let error           = Color("Colors/Status/Error")
}